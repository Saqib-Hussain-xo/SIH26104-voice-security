# Scripts

This folder is reserved for utility scripts. At the moment the active utilities are located elsewhere.

## Current status

- No runtime scripts required in this folder for the MVP.
- Main utilities:
  - evaluation/evaluate_dataset.py — inference/evidence script
  - backend/tests/test_api.py — backend tests

## Future

Add reusable utilities here (setup helpers, dataset prep, demo-data scripts) if needed.
